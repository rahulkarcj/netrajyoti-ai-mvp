# Keep repository text files consistent across Windows, macOS, and Linux.
* text=auto eol=lf

# Windows scripts need CRLF when they are added later.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
